workflows:
 ios-workflow:
   name: UHRI iOS 26
   instance_type: mac_mini_m2
   max_build_duration: 60
   environment:
     xcode: latest
   scripts:
     - name: Build iOS 26
       script: |
         xcodebuild -version
         xcodebuild -list -project "UHRI.xcodeproj"
         mkdir -p UHRI
         echo "<html></html>" > UHRI/index.html
         echo "<html></html>" > index.html
         xcodebuild build -project "UHRI.xcodep
Sent from my Patrick Uhri iPhone
Repeal the 16th Amendment!
